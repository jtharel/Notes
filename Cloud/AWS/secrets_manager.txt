aws secretsmanager list-secrets --query "SecretList[].Name" --output table 

aws secretsmanager get-secret-value --secret-id <SECRET_ID> --profile <PROFILE_NAME>

aws secretsmanager list-secrets --query "SecretList[].Name" --profile <PROFILE_NAME> --output table | awk '{print $2}' > secrets.txt

for X in $(cat secrets.txt); do echo "secret $X: "; aws secretsmanager get-secret-value --secret-id $X --profile <PROFILE_NAME>; echo '*************'; done 


I think I can do aws ssm as well as aws secretsmanager 

# will return everyone who can view SecretManager! If pmapper data has already been built
	pmapper query -s 'who can do secretsmanager:GetSecretValue'


# if aquired access key and access secret key:
	aws iam list-attached-user-policies --user-name <USRENAME>

	aws iam list-user-policies --user-name <USERNAME>


# check Secret rotation configuration:
  aws secretsmanager list-secrets --query "SecretList[*].{Name:Name,RotationEnabled:RotationEnabled,LastRotated:LastRotatedDate}" --output table --region <your-region> --profile <PROFILE_NAME>


