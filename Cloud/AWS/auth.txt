aws configure --profile <profile>
aws sts get-caller-identity --profile <profile>


aws configure sso
	SSO session name (Recommended): blahsso
	SSO start URL [None]: https://identitycenter.amazonaws.com/ssoins-7223ae7b79f760d3
	SSO region [None]: us-east-1
	SSO registration scopes [sso:account:access]:
aws sso login --profile <profile_name>


export AWS_PROFILE=<profile> #do this to set the default profile
export AWS_ACCESS_KEY_ID=
export AWS_SECRET_ACCESS_KEY=
export AWS_SESSION_TOKEN=	#only needed for temp keys


aws sts assume-role --role-arn <role ARN> --role-session-name <session name> --profile <base profile>

Then export the returned temporary credentials or write them into a new AWS profile for convenience, and use it to interact with AWS services under the assumed role.
